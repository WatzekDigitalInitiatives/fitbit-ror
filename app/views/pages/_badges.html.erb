<div class="row">
    <div class="col s3">
        <div class="achievement-heading">
            Daily Steps
        </div>
        <div class="divider"></div>
    </div>
    <div class="col s3">
        <div class="achievement-heading">
            Lifetime Distance
        </div>
        <div class="divider"></div>
    </div>
    <div class="col s3">
        <div class="achievement-heading">
            Daily Floors
        </div>
        <div class="divider"></div>
    </div>
    <div class="col s3">
        <div class="achievement-heading">
            Lifetime Floors
        </div>
        <div class="divider"></div>
    </div>
</div>
<div class="row">
    <div class="col s3">
        <p class="badge-text">
            <% if @badges["Daily Steps"] %>
            <%= image_tag(image_path(@badges["Daily Steps"]["image125px"])) %><br/><br/>
            <b><%=  @badges["Daily Steps"]["shortName"] %></b><br/><br/>
            <i><%=  @badges["Daily Steps"]["description"] %></i><br/><br/>
            <p class="badge-desc">
                <%=  @badges["Daily Steps"]["mobileDescription"] %>
            </p>
        <% else %>
            No badges yet.
            <% end %>
        </p>
    </div>
    <div class="col s3">
        <p class="badge-text">
            <% if @badges["Lifetime Distance"] %>
            <%= image_tag(image_path(@badges["Lifetime Distance"]["image125px"])) %><br/><br/>
            <b><%=  @badges["Lifetime Distance"]["shortName"] %></b><br/><br/>
            <i><%=  @badges["Lifetime Distance"]["description"] %></i><br/><br/>
            <p class="badge-desc">
                <%=  @badges["Lifetime Distance"]["mobileDescription"] %>
            </p>
        <% else %>
            No badges yet.
            <% end %>
        </p>
    </div>
    <div class="col s3">
        <p class="badge-text">
            <% if @badges["Daily Climb"] %>
            <%= image_tag(image_path(@badges["Daily Climb"]["image125px"])) %><br/><br/>
            <b><%=  @badges["Daily Climb"]["shortName"] %></b><br/><br/>
            <i><%=  @badges["Daily Climb"]["description"] %></i><br/><br/>
            <p class="badge-desc">
                <%=  @badges["Daily Climb"]["mobileDescription"] %>
            </p>
        <% else %>
            No badges yet.
            <% end %>
        </p>
    </div>
    <div class="col s3">
        <p class="badge-text">
            <% if @badges["Lifetime Climb"] %>
            <%= image_tag(image_path(@badges["Lifetime Climb"]["image125px"])) %><br/><br/>
            <b><%=  @badges["Lifetime Climb"]["shortName"] %></b><br/><br/>
            <i><%=  @badges["Lifetime Climb"]["description"] %></i><br/><br/>
            <p class="badge-desc">
                <%=  @badges["Lifetime Climb"]["mobileDescription"] %>
            </p>
        <% else %>
            No badges yet.
            <% end %>
        </p>
    </div>
</div>
